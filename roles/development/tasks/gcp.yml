---

- name: Install google-cloud-sdk, kubectl
  pacman:
    name:
      - "{{ role_path }}/files/google-cloud-sdk-294.0.0-1-x86_64.pkg.tar.xz"
      - kubectl
    state: present
  become: yes

- name: Config fish for kubectl
  copy:
    src: fish/kubectl.fish
    dest: "{{ ansible_user_dir }}/.config/fish/conf.d/"
  tags: fish
